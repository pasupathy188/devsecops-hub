# These CVEs are confirmed to originate from npm's own bundled internal
# tooling (shipped inside the node:22-alpine base image) or other build
# tooling, not from this project's actual application dependencies.
# Verified via `docker run ... find` showing the package path resolves
# under npm's internal node_modules, not the app's own dependency tree.

CVE-2026-9496
CVE-2026-73566
